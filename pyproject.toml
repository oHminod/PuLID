[build-system]
requires = ["hatchling>=1.27"]
build-backend = "hatchling.build"

[project]
name = "pulid-app"
version = "0.1.0"
description = "Pipeline Python autonome pour PuLID et SDXL"
readme = "README.md"
requires-python = ">=3.11,<3.14"
dependencies = [
  "insightface==0.7.3",
  "numpy>=1.26,<3",
  "onnxruntime>=1.20,<2",
  "opencv-python>=4.10,<5",
  "Pillow>=11,<13",
  "PyYAML>=6.0.2,<7",
  "rich>=13.9,<15",
  # InsightFace 0.7.3 appelle encore SimilarityTransform.estimate().
  "scikit-image>=0.22,<0.26",
  "torch>=2.13,<2.14",
]

[project.optional-dependencies]
# Dépendances complémentaires installées lorsque les phases d'inférence commencent.
inference = [
  "accelerate>=1.14,<1.15",
  "diffusers>=0.39,<0.40",
  "huggingface-hub>=1.27,<1.28",
  "safetensors>=0.8,<0.9",
  "torchvision>=0.28,<0.29",
  "transformers>=5.15,<5.16",
]
pulid = [
  "einops>=0.8,<0.9",
  "facexlib>=0.3,<0.4",
  "ftfy>=6.3,<7",
  "timm>=1.0,<2",
]
server = [
  "fastapi>=0.115,<1",
  "python-multipart>=0.0.20,<1",
  "uvicorn>=0.34,<1",
]
embeddings = [
  "llama-cpp-python>=0.3.16,<0.4",
]
dev = [
  "httpx>=0.28,<1",
  "pytest>=8.3,<10",
]

[project.scripts]
pulid-gen = "pulid_app.cli:main"
pulid-inspect-models = "pulid_app.cli:inspect_main"
pulid-install = "pulid_app.installer:main"
pulid-server = "pulid_app.server:main"

[tool.hatch.build.targets.wheel]
packages = ["src/pulid_app"]

[tool.pytest.ini_options]
addopts = "-ra"
testpaths = ["tests"]
markers = [
  "unit: test rapide, isolé du réseau, du SSD externe et des modèles lourds",
  "integration: test plusieurs composants avec les modèles locaux configurés",
  "slow: test charge un checkpoint lourd ou exécute une génération",
  "gpu: test nécessitant un accélérateur MPS ou CUDA",
]
